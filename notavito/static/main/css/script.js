const screenWidth = screen.width;
const screenHeight = screen.height;
var body = document.getElementById('body');
body.width = screenWidth;
body.height = screenHeight
new SimpleBar(document.querySelector('.my-container'));